// Re-exported for backwards compatibility — the full curated set now lives in
// lib/iconCatalog.ts, shared by both the account and category icon pickers.
export { ICON_GROUPS as ACCOUNT_ICON_GROUPS } from "./iconCatalog";
